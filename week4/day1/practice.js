// console.log("Hello World")

// console.table()

// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    // }


// const tipAmount = 

// const tipcalculator = (totalBill) => {
//     let tipAmount = if (totalBill > 50) => {
//         totalBill 
//     }

//     let mealTotal = totalBill * tipAmount;
//     return mealTotal + total; 
// }; 



const makeAReservation = (VIPStatus) => {
    let reservationList = []
    if VIPStatus == 1, push(makeAReservation)
    if VIPStatus == 2, push(makeAReservation)
    if VIPStatus == 
}
